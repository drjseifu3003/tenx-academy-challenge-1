{
  "name": "address-validator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p .",
    "test": "jest --coverage",
    "typecheck": "tsc -p . --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "ts-jest": "^29.1.0",
    "jest": "^29.6.0",
    "@types/jest": "^29.5.3"
  }
}
